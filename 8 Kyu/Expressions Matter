function expressionMatter(a, b, c) {
  
  let result1 = a * (b + c);
  let result2 = a * b * c;
  let result3 = (a + b) * c;
  let result4 = a + b + c;
  
  let arr = [result1, result2, result3, result4];
  let finalResult = arr.sort((a, b) => b-a)[0];
  
  return finalResult;
}
