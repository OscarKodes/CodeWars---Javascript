function countSheeps(arrayOfSheep) {
  
  let sum = 0;
  
  for (let i = 0; i < arrayOfSheep.length; i++) {
  
    let element = arrayOfSheep[i];
    
    if (element) {
    
      sum++;
    }
  }
  
  return sum;
}
