const multiplicationTable = size => new Array(size).fill(0).map((_, i) => createRow(size, i + 1)); 

const createRow = (size, multiplier) => new Array(size).fill(0).map((_, i) => (i + 1) * multiplier);
