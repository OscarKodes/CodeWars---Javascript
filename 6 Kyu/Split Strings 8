function solution(str){
  
  let result = [];
  
  for (let i = 0; i < str.length; i += 2) {
    
    let pair = str.slice(i, i+2);
    
    result.push(pair.length === 2 ? pair : pair + "_");
  }
  
  return result;
}
