function twosDifference(input){
   
  let result = [];

  for (let i = 0; i < input.length; i++) {
  
    let num = input[i];

    if (input.indexOf(num + 2) > -1) {
    
      result.push([num, num + 2]);
    }
  }

  return result;
}
