function range(start, end, step=1) {
  
  if (end === undefined) {
    end = start;
    start = 0;
  }
  
  let result = [];
  let num = start;
  
  for (let i = start; i < end; i += step || 1) {
    
    result.push(num);
    num += step;
  }
  
  return result;
}
