function findUniq(arr) {
  
  let repeatNum;
  
  if (arr[0] === arr[1]) {
  
    repeatNum = arr[0];
  }
  
  else if (arr[1] === arr[2]) {
  
    repeatNum = arr[1];
  }
  
  else {
  
    repeatNum = arr[2];
  }
  
  return arr.filter(num => num !== repeatNum)[0];
}
