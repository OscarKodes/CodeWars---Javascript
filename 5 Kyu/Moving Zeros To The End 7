// https://www.codewars.com/kata/52597aa56021e91c93000cb0

function moveZeros(arr) {
  
  let zeros = [];
  
  return arr.slice().filter(e => {
    
    if (e === 0) {
      zeros.push(0)
    };
    
    return e !== 0;
  }).concat(zeros);
}
