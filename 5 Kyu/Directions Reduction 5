function dirReduc(a){

  let r = [];
  let o = {"N":"S","S":"N","W":"E","E":"W"};
  
  a.map(d => {
  
    if (r.length > 0 && r[r.length - 1][0] === o[d[0]]) {
      r.pop()
    }
    
    else {
      r.push(d)
    }
  })
  
  return r;
}
