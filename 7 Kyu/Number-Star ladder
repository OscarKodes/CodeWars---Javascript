function pattern(n){
 
  let result = [];
  
  for (let i = 0; i < n; i++) {
    
    let line = "1" + "*".repeat(i);
    let endNum = i > 0 ? i + 1 + "" : "";
    
    result.push(line + endNum);
  }
  
  return result.join("\n");
}
