function nextPerfectSquare(n){
  
  let sqrtN = Math.sqrt(n);
  let nextRoot = 0;
  
  if (sqrtN % 1 === 0) {
    nextRoot = sqrtN + 1;
  }
  
  else if (n > 0) {
    nextRoot = Math.ceil(sqrtN);
  }
  
  return nextRoot ** 2;
}
