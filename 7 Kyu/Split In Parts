const splitInParts = (s, partLength) => {
  
  let result = [];
  
  while (s.length > 0) {
  
    result.push(s.slice(0, partLength));
    s = s.slice(partLength);
  }
  
  return result.join(" ");
}
