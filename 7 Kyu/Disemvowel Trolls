function disemvowel(str) {

  let vowels = "aeiouAEIOU";
  
  let newStr = "";
  
  for (let i = 0; i < str.length; i++) {
  
    let char = str[i];
    
    if (!vowels.includes(char)) {
    
      newStr += char;
    }
  }
  
  return newStr;
}
